version: 1
directive: "Optimize distributed LLM inference throughput across pipeline-parallel stages, finding the best combination of stage count, compression, and latency tolerance"

agent:
  backend: claude-code

strategy:
  moonshot_ratio: 0.5
  enforce: soft

targets:
  min_campaigns: 10
  min_experiments: 100
